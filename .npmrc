# Required: @strapi/design-system and @strapi/icons peer-depend on react ^17 || ^18,
# but @types/react@^19 and react-intl@8.x expect react ^19. This mismatch is cosmetic —
# Strapi 5 works fine with React 18 at runtime. Remove once Strapi updates peer deps.
legacy-peer-deps=true
